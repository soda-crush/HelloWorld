<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PlogCohow">

	<select id="selectMyQuestion" parameterType="String" resultType="kh.hello.dto.CodeQuestionDTO">
		select seq, division, title, writer, writeDate, viewCount from codeQuestion where id=#{id}
	</select>
	
	<select id="selectMyReply" parameterType="String" resultType="kh.hello.dto.CodeQuestionDTO">
		select seq, division, c.title, c.writer, c.writeDate, viewCount from (select queseq from codeReply where id=#{id}) rep inner join codeQuestion c on rep.queseq = c.seq
	</select>

</mapper>